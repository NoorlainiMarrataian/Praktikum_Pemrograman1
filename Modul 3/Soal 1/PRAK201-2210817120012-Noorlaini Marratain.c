#include <stdio.h>

int main()
{
    char na[19], ni[13], k[1], ttl[18], a[39], h[7], no[12];

    printf("Nama                :");
    gets(na);
    printf("NIM                 :");
    gets(ni);
    printf("Kelas Paralel       :");
    gets(k);
    printf("Tempat/Tanggal Lahir:");
    gets(ttl);
    printf("Alamat              :");
    gets(a);
    printf("Hobby               :");
    gets(h);
    printf("No. HP              :");
    gets(no);
    return 0;
}
